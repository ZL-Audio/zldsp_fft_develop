name: Build Test

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build_test:
    name: ${{ matrix.name }} Build
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-latest
          - name: macOS
            os: macos-latest
          - name: Windows
            os: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Show CPU Info (Linux)
        if: runner.os == 'Linux'
        run: lscpu

      - name: Show CPU Info (macOS)
        if: runner.os == 'macOS'
        run: sysctl -n machdep.cpu.brand_string

      - name: Show CPU Info (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: Get-CimInstance -ClassName Win32_Processor | Select-Object Name, NumberOfCores, NumberOfLogicalProcessors

      - name: Build AVX2 Double (Linux)
        if: runner.os == 'Linux'
        run: |
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=ON -DUSE_DOUBLE=ON
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build AVX2 (Linux)
        if: runner.os == 'Linux'
        run: |
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=ON -DUSE_DOUBLE=OFF
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build SSE4 Double (Linux)
        if: runner.os == 'Linux'
        run: |
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=OFF -DUSE_DOUBLE=ON
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build SSE4 (Linux)
        if: runner.os == 'Linux'
        run: |
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=OFF -DUSE_DOUBLE=OFF
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build and run dispatch examples (Linux)
        if: runner.os == 'Linux'
        run: |
          cmake -B build-dispatch -DSTATIC_DISPATCH_EXAMPLE=ON -DDYNAMIC_DISPATCH_EXAMPLE=ON -DCMAKE_BUILD_TYPE=Release
          cmake --build build-dispatch --config Release --target zldsp_fft_static_dispatch_example zldsp_fft_dynamic_dispatch_example
          ./build-dispatch/zldsp_fft_static_dispatch_example 5 20
          ./build-dispatch/zldsp_fft_dynamic_dispatch_example 5 20

      - name: Build NEON Double (macOS)
        if: runner.os == 'macOS'
        run: |
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=OFF -DUSE_DOUBLE=ON
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build NEON (macOS)
        if: runner.os == 'macOS'
        run: |
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=OFF -DUSE_DOUBLE=OFF
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build and run dispatch examples (macOS)
        if: runner.os == 'macOS'
        run: |
          cmake -B build-dispatch -DSTATIC_DISPATCH_EXAMPLE=ON -DDYNAMIC_DISPATCH_EXAMPLE=ON -DCMAKE_BUILD_TYPE=Release
          cmake --build build-dispatch --config Release --target zldsp_fft_static_dispatch_example zldsp_fft_dynamic_dispatch_example
          ./build-dispatch/zldsp_fft_static_dispatch_example 5 20
          ./build-dispatch/zldsp_fft_dynamic_dispatch_example 5 20

      - name: Build AVX2 Double (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          for /f "usebackq tokens=*" %%i in (`"%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set "VS_PATH=%%i"
          if not defined VS_PATH exit /b 1
          call "%VS_PATH%\VC\Auxiliary\Build\vcvars64.bat"
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=ON -DUSE_DOUBLE=ON
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build AVX2 (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          for /f "usebackq tokens=*" %%i in (`"%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set "VS_PATH=%%i"
          if not defined VS_PATH exit /b 1
          call "%VS_PATH%\VC\Auxiliary\Build\vcvars64.bat"
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=ON -DUSE_DOUBLE=OFF
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build SSE4 Double (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          for /f "usebackq tokens=*" %%i in (`"%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set "VS_PATH=%%i"
          if not defined VS_PATH exit /b 1
          call "%VS_PATH%\VC\Auxiliary\Build\vcvars64.bat"
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=OFF -DUSE_DOUBLE=ON
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build SSE4 (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          for /f "usebackq tokens=*" %%i in (`"%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set "VS_PATH=%%i"
          if not defined VS_PATH exit /b 1
          call "%VS_PATH%\VC\Auxiliary\Build\vcvars64.bat"
          cmake -B build -DBUILD_TEST=ON -DENABLE_ZLDSP=ON -DUSE_AVX2=OFF -DUSE_DOUBLE=OFF
          cmake --build build --config Release --target zldsp_fft_build_test

      - name: Build and run dispatch examples (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          for /f "usebackq tokens=*" %%i in (`"%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set "VS_PATH=%%i"
          if not defined VS_PATH exit /b 1
          call "%VS_PATH%\VC\Auxiliary\Build\vcvars64.bat"
          cmake -B build-dispatch -DSTATIC_DISPATCH_EXAMPLE=ON -DDYNAMIC_DISPATCH_EXAMPLE=ON
          cmake --build build-dispatch --config Release --target zldsp_fft_static_dispatch_example zldsp_fft_dynamic_dispatch_example
          build-dispatch\Release\zldsp_fft_static_dispatch_example.exe 5 20
          build-dispatch\Release\zldsp_fft_dynamic_dispatch_example.exe 5 20
